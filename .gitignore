# Secrets — never commit
.env
.env.*
!.env.example

# Large binary source assets (videos)
*.mp4
*.m4a

# OS / editor
.DS_Store
Thumbs.db
node_modules/
.next/

.vercel
.env*

# TypeScript incremental build cache — churns on every build
tsconfig.tsbuildinfo

# Generated client deliverables — regenerate with scripts/client-report.mjs
*.pdf
